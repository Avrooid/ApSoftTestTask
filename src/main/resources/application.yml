spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/apSoftTestTask
    username: postgres
    password: poiiop5885
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect